# create a virtual environment
python -m venv venv

# enter the virtual environment
source venv/bin/activate

# install the required libraries
pip install -r requirements.txt

# you should now be ready to run plot_vita_pcapng.py
